{
  "type": "array",
  "items": {
    "type": "object",
    "required": [
      "title",
      "organiser",
      "blurb",
      "description",
      "guid",
      "activity_or_event",
      "url",
      "price_display_teaser",
      "price_display",
      "price",
      "is_free",
      "min_price",
      "max_price",
      "age_group_display",
      "min_age",
      "max_age",
      "datetime_display_teaser",
      "datetime_display",
      "start_datetime",
      "end_datetime",
      "venue_name",
      "categories",
      "images"
    ],
    "properties": {
      "id": {
        "type": "integer",
        "description": "Unique event ID"
      },
      "title": {
        "type": "string",
        "minLength": 40,
        "maxLength": 50,
        "description": "The title of the event. Do not mention the location or date or time of the event here."
      },
      "organiser": {
        "type": "string",
        "description": "The organisation that organise the event. Put multiple organisers if needed."
      },
      "blurb": {
        "type": "string",
        "minLength": 40,
        "maxLength": 60,
        "description": "A short description of the event to put on the event card"
      },
      "description": {
        "type": "string",
        "description": "Describe the event within the word limits. .Talks about the activities and target audience. Write in family friendly tone.",
        "minLength": 450,
        "maxLength": 700
      },
      "guid": {
        "type": "string",
        "description": "The guid needs to be a valid URL links that DOES NOT HAVE additional character on it for each event. This value should **NOT** BE UNIQUE TO EACH EVENT."
      },
      "activity_or_event": {
        "type": "string",
        "maxLength": 1,
        "description": "Event when the feed is time specific or one - off. Activity is when the feed is available all year round.",
        "enum": [
          "event",
          "activity"
        ]
      },
      "url": {
        "type": "string",
        "description": "The URL of the event. Extract from URL, not the GUID. Ensure that the URL is valid, does not return 404 error"
      },
      "price_display_teaser": {
        "type": "string",
        "description": "To be shown at the feed. If there is paid options, use 'From $' only.",
        "enum": [
          "Free + Paid options",
          "Free",
          "From $"
        ]
      },
      "price_display": {
        "type": "string",
        "description": "The price of the event in a display format, e.g. $100 or free. Limit to JUST 1 sentence, not more than 10 words. Ensure that it is in Singapore Dollar currency."
      },
      "price": {
        "type": "number",
        "description": "If there are multiple prices, choose the LOWEST price. If the event has both free tier and paid options, USE the lowest price of paid option. Ensure that it is in Singapore Dollar currency."
      },
      "is_free": {
        "type": "boolean",
        "description": "Whether the event is free (true if price is 0, false otherwise)"
      },
      "min_price": {
        "type": "number",
        "description": "The minimum price for the event in Singapore Dollar currency. If free, use 0. If there are multiple price tiers, use the lowest price."
      },
      "max_price": {
        "type": "number",
        "description": "The maximum price for the event in Singapore Dollar currency. If there's only one price, this should equal min_price. If free, use 0."
      },
      "age_group_display": {
        "type": "string",
        "description": "The age group of the event in a display format, e.g. \"18 months to 3 years\" or \"3 to 12 years\" or \"all ages\""
      },
      "min_age": {
        "type": "number",
        "description": "The minimum age of the event in years"
      },
      "max_age": {
        "type": "number",
        "description": "The maximum age of the event in years"
      },
      "datetime_display_teaser": {
        "type": "string",
        "description": "Please display **dates/days** on the feed card, using the **DD-MMM format** (e.g., '28 Jun - 29 Aug', '19 Jan'), keeping it **short** (e.g., 'Available daily','Available daily except Tue', 'Recurring weekends'), and **excluding time**."
      },
      "datetime_display": {
        "type": "string",
        "description": "The field must capture the complete, human-readable date and time description of an event, extracted verbatim from the input text. This includes the primary timing patterns and any closely associated contextual details that are part of the time expression. This field typically reflects one of these patterns: Recurring events: e.g., 'Weekends 12:00pm - 1:00pm', 'Daily (except Tuesday, Thursday) 7:00am - 9:00am'. Single specific dates: e.g., '18th Apr 2025, 9:00am - 5:00pm', '25th December 2024, all day'. Defined date ranges: e.g., 'From 19 Jul 2025 to 19 Oct 2025, 1:00pm - 10:00pm', 'From now to 21st Dec 2025, 5:00pm - 7:00pm'. Important Consideration for Edge Cases: If additional contextual information (e.g., specific locations for different times, notes about exceptions or special conditions) is embedded within the date/time phrasing and contributes to its overall meaning for display, include it."
      },
      "start_datetime": {
        "type": "string",
        "description": "The start date and time of the event in ISO 8601 format in SGT timezone. If it does not have a start date, return the date at 1970-01-01T00:00:00+08:00",
        "format": "date-time"
      },
      "end_datetime": {
        "type": "string",
        "description": "The end date and time of the event in ISO 8601 format, in SGT timezone. If it does not have an end date, return the date at 9999-12-31T23:59:59+08:00",
        "format": "date-time"
      },
      "venue_name": {
        "type": "string",
        "description": "The name of the venue of the event"
      },
      "address_display": {
        "type": "string",
        "description": "The complete address of the venue. This is the only address field - capture all address information here."
      },
      "planning_area": {
        "type": "string",
        "description": "Singapore URA planning area for the venue"
      },
      "region": {
        "type": "string",
        "description": "Singapore URA region for the venue"
      },
      "categories": {
        "type": "array",
        "description": "Event categories to must include at least one category",
        "minItems": 1,
        "items": {
          "type": "string",
          "enum": [
            "Indoor Playground",
            "Attraction",
            "Outdoor Playground",
            "Mall related",
            "Kids-friendly dining"
          ]
        }
      },
      "keyword_tag": {
        "type": "string",
        "description": "Identify relevant search keywords and phrases for the given content. Return 5-12 concise, user-searchable terms separated by commas (e.g., 'playground, kids, indoor, fun, activities')."
      },
      "label_tag": {
        "type": "string",
        "description": "User-editable label for custom annotations or manual tags",
        "default": ""
      },
      "images": {
        "type": "array",
        "description": "Array of image objects for the event",
        "items": {
          "type": "object",
          "properties": {
            "url": {
              "type": "string",
              "description": "Direct image URL (.jpg/.jpeg/.png/.webp)"
            },
            "source_credit": {
              "type": "string",
              "description": "Organiser or venue name for image attribution"
            },
            "filename": {
              "type": "string",
              "description": "Short filename for the image"
            },
            "local_path": {
              "type": "string",
              "description": "Local path for the image"
            }
          },
          "required": ["url", "source_credit", "filename","local_path"]
        },
        "default": []
      }
    }
  }
}